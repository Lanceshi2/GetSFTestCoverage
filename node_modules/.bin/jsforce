#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jsforce/bin/jsforce" "$@"
  ret=$?
else 
  node  "$basedir/../jsforce/bin/jsforce" "$@"
  ret=$?
fi
exit $ret
